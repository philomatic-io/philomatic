{
  "name": "philomatic-obsidian",
  "version": "0.0.0",
  "private": true,
  "description": "Philomatic entities embedded in Obsidian notes — a pure HTTP client of the self-serve server (plan OB1)",
  "type": "module",
  "scripts": {
    "build": "tsc --noEmit && node esbuild.config.mjs production",
    "dev": "node esbuild.config.mjs",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "esbuild": "^0.28.1",
    "obsidian": "^1.7.2",
    "typescript": "^5.7.3"
  }
}
